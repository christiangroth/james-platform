* Aggregations can now be added, edited and removed per entity in the version editor, with immediate validation feedback.
* Test installations of a draft recompute their aggregation values when an aggregation is changed.
