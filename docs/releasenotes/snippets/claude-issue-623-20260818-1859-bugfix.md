* Deleting a user now also removes that user's installed apps and their data, which was previously left behind.
* Deleting a user now runs in the background instead of blocking the request, so users with a lot of installed app data no longer risk timing out.
* The user management page shows a "wird gelöscht" notice while a user deletion is still being processed in the background.
