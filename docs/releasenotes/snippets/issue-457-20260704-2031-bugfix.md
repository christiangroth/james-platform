* Shortened the constraint hint texts shown under data entry fields and fixed their order so the minimum is always shown before the maximum.
* Removed the constraint info icon next to field labels, since it didn't work on touch devices; all constraint hints (including pattern, step and unique key) are now shown directly under the input instead.
