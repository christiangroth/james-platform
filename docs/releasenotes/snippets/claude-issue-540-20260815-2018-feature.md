* Developers can now deactivate an app from its overview page; existing installations keep working unaffected, the developer just gets shown how many active installations exist.
* Users now see a hint when an installed app has been deactivated by its developer, both on the dashboard tile and as a banner on the app's pages, including its import job pages.
