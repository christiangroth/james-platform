* Added a "Quelldaten anzeigen" side panel to the Filter and Mapping steps of the Data Import wizard, showing the detected source schema (fields, types, mandatory flags) plus value ranges and string length hints, without leaving the current step.
* The import wizard's step indicator now shows just the icon and step number on narrow screens, keeping it usable on mobile.
