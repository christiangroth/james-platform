* Increased Gradle daemon heap size to 2 GB for faster builds.
* Enabled Gradle configuration cache to speed up repeated builds.
* Enabled Gradle build-cache writes in CI for all branches to avoid full rebuilds on subsequent runs.
