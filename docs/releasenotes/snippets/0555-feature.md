* Import dry-run: the target entity's name is now shown in the breadcrumbs, so it stays visible while working through mapping and dry-run.
* Import dry-run: the four counts (total, valid, invalid, discarded) are now shown in one row, in a clearer order, with two per row on small screens.
* Import dry-run: the "Accept" button now sits right below the list of problems, so it can be reached without scrolling past a long fan-in report.
* Import dry-run: records discarded by design (fan-in) are now summarized by field and reason instead of listing every single record, cutting down noise for large imports.
