* Aggregations are now recomputed after generating test data and after manually upgrading an installed app.
* Aggregation values of an installation are removed when the installation, its app or its user is deleted.
