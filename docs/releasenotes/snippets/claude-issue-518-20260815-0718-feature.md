* Added a dedicated page per import job, with the job's id in the URL, showing status, target app installation, target entity, dates, and data path selection.
* The import job page visualizes and navigates the Datenpfad / Mapping / Dry-Run steps, replacing the scattered action buttons and buried "back" links from before.
* The imports overview is now a top-level page (next to Connections) listing status, target app installation, target entity, created at and last action across all apps; data path and actions now live only on the import job's own page.
* Breadcrumbs across the imports, import job, mapping and dry-run pages are now consistent with this structure.
