* Property Units (measurement unit on `Long`/`Double` fields, e.g. distance or time) can now be configured in the property editor, choosing a category, storage granularity, and default granularity.
* Number fields with a configured unit now show a granularity dropdown when entering or editing data, with a text-mode toggle for typing values like "15km 400m" directly.
