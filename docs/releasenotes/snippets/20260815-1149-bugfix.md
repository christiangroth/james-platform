* Import dry-run: invalid objects are shown expanded automatically; valid objects are shown as a reviewable table of target values instead of individual accordions.
* Import mapping and dry-run breadcrumbs no longer nest under Quelle, since it is a sibling step, not their parent.
* Import table made more compact: shorter column headers, a new Connection column, and blank headers for the status and actions columns.
* New Connection button is now icon-only.
* Connections table: the test and edit buttons are now blue icon-only buttons, and the URL/Bearer-Token columns and fields were renamed to Base URL/Auth.
