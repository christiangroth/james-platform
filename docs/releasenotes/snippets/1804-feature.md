* The "Import Jobs" and "Import Definitions" pages have been merged into a single Import overview, grouping each import's jobs under its source definition.
* Finished one-time imports no longer clutter the overview; each import definition now has a "Historie" button showing its past runs.
* A scheduled import definition now shows its next scheduled run time directly in the overview.
