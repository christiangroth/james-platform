* The "New Import" button now shows only a plus icon instead of text.
* Connections now live logically under Imports: the URL, breadcrumbs and the button to manage them only appear on the Imports page instead of the dashboard.
* The imports table now has an "Aktionen" column with an icon-only delete button, confirmed via a modal.
* Clicking anywhere on an import row now opens it.
