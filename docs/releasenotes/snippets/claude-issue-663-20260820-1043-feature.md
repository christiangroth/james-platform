* Scheduled data imports can now be configured to send a Slack notification after every automated run, summarizing how many objects were saved and discarded.
* If an automated run is aborted because the source data no longer matches the previously imported structure, the Slack notification explicitly calls out that the import's filter/mapping needs manual review.
