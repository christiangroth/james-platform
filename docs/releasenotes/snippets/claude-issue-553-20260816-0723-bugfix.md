* Inactive apps now show a gray "Inactive" badge on the developer dashboard.
* Developer actions on an inactive app (creating, editing or deleting a draft version, and publishing a release) are now blocked, both as disabled buttons and enforced on the backend.
* The delete-app confirmation now shows the current number of installations up front, instead of only after a failed delete attempt.
