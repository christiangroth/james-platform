* The delete action for an import job is now offered only in the imports table, not on the job's own pages.
* Renamed "Datenpfad" to "Quelle" as the label for the source-selection step.
* The Quelle/Mapping/Dry-Run steps are now shown as a full-width wizard progress bar instead of three separate buttons.
* Clicking the selected data path now expands the detected JSON structure inline instead of opening a modal, and shows the complete structure of the downloaded document, not just the fields of the selected array.
* Breadcrumbs now show an explicit "Quelle" entry, consistent with how the Mapping and Dry-Run steps are already shown.
* The dry-run "Ersetzen" and "Hinzufügen" buttons are now right-aligned and carry an icon in addition to their text.
* The valid/invalid/discarded detail panels on the dry-run page now behave like a real accordion: opening one closes the others.
