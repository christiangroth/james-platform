* Data imports now use reusable connections: save a source's URL and optional Bearer token once under "Connections", then start as many import jobs against it as you like without re-entering credentials.
* Connections can be tested, edited, and deleted independently of any import job, and stay available until you delete them yourself.
* When starting a new import, you now pick the target entity type up front instead of choosing it later during mapping.
