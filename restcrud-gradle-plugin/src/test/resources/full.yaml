packageName: "de.foo.bar"

datamodel:
  - name: "Foo"
    endpoint: "/api/foos"
    attributes:
      - "key id Long"
      - "description String?"
  - name: "Bar"
    endpoint: "/api/bars"
    attributes:
      - "key id Long"
      - "key version Long"
  - name: Hidden
    attributes:
      - "name String"