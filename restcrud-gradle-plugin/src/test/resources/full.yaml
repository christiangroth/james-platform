packageName: "de.foo.bar"

model:
  - name: "Foo"
    endpoint: "/api/foos"
    attributes:
      - "id Long"
      - "description String?"
  - name: "Bar"
    endpoint: "/api/bars"
    attributes:
      - "id Long"
      - "version Long"
  - name: Hidden
    attributes:
      - "name String"